export class {{pascalCase entityName}}Entity {
  id?: number

  constructor(data: Partial<{{pascalCase entityName}}Entity>, id?: number) {
    this.id = id;
    Object.assign(this, data);
  }

  validate(): boolean {
    return true;
  }

  sanitize(): void {
  }
}