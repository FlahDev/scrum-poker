import { ComponentStory, ComponentMeta } from '@storybook/react'

import { {{pascalCase componentName}} } from './component'

export default {
  title: '{{pascalCase componentName}}',
  component: {{pascalCase componentName}}
} as ComponentMeta<typeof {{pascalCase componentName}}>

//👇 We create a “template” of how args map to rendering
const Story: ComponentStory<typeof {{pascalCase componentName}}> = (args) => <{{pascalCase componentName}} {...args} />

export const Primary{{pascalCase componentName}} = Story.bind({})
export const Secondary{{pascalCase componentName}} = Story.bind({})

Primary{{pascalCase componentName}}.args = {}

Secondary{{pascalCase componentName}}.args = {}
