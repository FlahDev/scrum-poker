export interface CreateUserVars {
  data: {
    name: string
    email: string
  }
}

export interface CreateUserData {
  createUser: {
    id: string
    name: string
    email: string
  }
}
